---
import Base from '../layouts/Base.astro';
import AguardandoConteudo from '../components/AguardandoConteudo.astro';
import Prosa from '../components/Prosa.astro';
import { buscarConteudo } from '../lib/dados.js';

// Deixou de ser estática: o texto agora vem do banco e a clínica edita pelo
// painel, sem precisar de mim para trocar uma frase.
export const prerender = false;

const conteudo = await buscarConteudo('artigos');
const titulo = conteudo?.titulo || 'Artigos';
---
<Base titulo={titulo} descricao="Textos escritos pela equipe da Arcádia Psicologia.">
  <div class="topo">
    <div class="dentro"><h1>{titulo}</h1></div>
  </div>
  <div class="corpo">
    {conteudo?.corpo
      ? <Prosa texto={conteudo.corpo} />
      : <AguardandoConteudo itens={["migração dos 4 artigos já publicados, com os endereços preservados", "definição de quem escreve e com que frequência"]} />}
  </div>
</Base>

<style>
  .topo { background: var(--creme); border-bottom: 1px solid var(--linha); }
  .dentro { max-width: 1120px; margin: 0 auto; padding: var(--e-5) var(--e-3); }
  h1 { font-size: var(--t-3xl); margin: 0; }
  .corpo { max-width: 1120px; margin: 0 auto; padding: var(--e-4) var(--e-3) 0; }
  @media (max-width: 720px) { h1 { font-size: var(--t-2xl); } }
</style>
